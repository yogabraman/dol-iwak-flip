<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="creative.station.dol_iwak">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Doliwak">
        <activity android:name=".menu.Sixth"></activity>
        <activity android:name=".menu.Fifth" />
        <activity android:name=".menu.Fourth" />
        <activity android:name=".menu.Third" />
        <activity android:name=".menu.Second" />
        <activity android:name=".menu.First" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>